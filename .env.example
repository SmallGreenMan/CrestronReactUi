# Copy to `.env.local` and adjust if the presenter runs elsewhere.
#
# WebSocket endpoint served by Altair.Demo/AltairPresenter.cs. The default matches
# Program.cs, which constructs `new AltairPresenter(ip, port, wsPort: 10001)`.
VITE_PRESENTER_WS_URL=ws://localhost:10001/
